import React from 'react'
import ReactDOM from 'react-dom'
import WindowManager from './window-manager'
import './index.scss'

const container = document.getElementById('app')
ReactDOM.render(<WindowManager />, container)
